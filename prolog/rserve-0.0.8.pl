:- <- {|r||
       overline = identity
       |}.

:- <- {|r||
       instead_of = function(instead, of)
           return(instead)
       |}.

:- <- {|r||
       denoting = function(x, y, desc)
           return(y)
       |}.

:- <- {|r||
       frac = `/`
       |}.

:- <- {|r||
       dfrac = `/`
       |}.
